GitHub Actions deployment is intentionally documented separately after the first Fly.io app is authenticated.

Required secret:
- FLY_API_TOKEN

Do not store ViaBTC API keys or the MIDAD gateway key in GitHub repository files or workflow source.